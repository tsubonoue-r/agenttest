{
  "name": "agenttest",
  "version": "1.0.0",
  "description": "Miyabi Framework - Autonomous Development Environment",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "state:transition": "tsx scripts/state-transition.ts",
    "agents:parallel:exec": "tsx scripts/agents-parallel-exec.ts",
    "report:weekly:issue": "tsx scripts/weekly-report.ts"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.2",
    "@octokit/graphql": "^7.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^1.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
